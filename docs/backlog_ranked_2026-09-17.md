# DMT2 Backlog — ROI ranking (value / effort) — 2026-09-17 (SUPERSEDED)

> **SUPERSEDED 2026-09-22 — `docs/backlog.html` is the canonical live backlog.**
> This file is a historical snapshot: a 2026-09-17 ROI ranking of the then-open items,
> complete only through item 62. Do not edit it and do not treat it as a source.
>
> The items themselves (and their cost/risk values) live in `docs/backlog.html`, which is
> now the complete list of items 1–70 and the only place the backlog is maintained.
>
> **The ROI ranking now lives in `backlog.html`.** The computed ROI ranking and suggested
> execution order that this snapshot introduced — value = P1:5 / P2:3 / P3:1, effort = cost + risk,
> ROI = value / effort, sorted descending — were folded into `docs/backlog.html` on 2026-09-22.
> That page now computes and displays an ROI column (with the value/effort inputs), is sortable
> by ROI, and has a one-click "Rank by ROI" view that floats the highest-ROI still-open items to
> the top. Use `backlog.html` for the live ranking; nothing here needs maintaining.
>
> The full original ranking table is preserved in git history. To read it:
> `git log --follow -p docs/backlog_ranked_2026-09-17.md`.

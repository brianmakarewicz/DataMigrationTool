# Blind Tranche Review — Views (2026-07-07)

Reviewer: blind subagent. Scope: all 170 files in db/views/ + install enrollment.
**Verdict: FAIL.** "Not a conforming implementation of the design but a snapshot of the drift
the design has already adjudicated": 73/170 views invalid against this repo's own tables tree
(installing silently under FORCE), the decided-DELETED report views enrolled, a resurrected
legacy table-as-view with fabricated data, the MiscReceipts drill chain on the wrong tables,
and the object registry hardcoded six times with five inconsistent vocabularies.

Proposed rules: 7, added to DMT_DESIGN.html section 7 in RED.

## Fixed now (2026-07-07)
| # | Finding | Fix |
|---|---------|-----|
| F1 (part) | 36 `dmt_*_imp_rpt_v.sql` — decided DELETED 2026-07-04, spec-confirmed invalid AND unreferenced | Files deleted; install.sql lines removed; verified by full install re-run. |

## Deferred with tracking
| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| F1 (rest) | HIGH | 37 summary/area views also invalid (INTEGRATION_ID joins) | **DEFER pending usage check** (spec's own open question: check APEX references) — verify against f155 export at Stage F; delete or repair then. Cannot delete blind: some back area pages. |
| F2 | HIGH | DMT_CONVERSION_MASTER_TBL view: legacy name, `_TBL` suffix on a view, fabricated zeros, RUN_ID-as-SCENARIO_ID | **DEFER to Stage C engine port** — referenced by legacy surfaces; delete with the INTEGRATION_ID/legacy sweep when the run-table columns are cleaned. Tracked P1. |
| F3 | HIGH | Run-metrics/dashboard/BIP-admin views structurally return false numbers (row status tested on runs; vocabulary-mismatched join) | **DEFER to Stage F dashboard rebuild** — these back the Dashboard page the design already marks "redesign needed"; do not port to new pages. Tracked. |
| F4 | HIGH | MiscReceipts drill chain reads orphaned RCV tables; INV_TRX tables have zero view coverage | **DEFER to MiscReceipts object port (Phase 4)** — catalog branches, detail views, and RCV drops land together. Same tracking as tables finding 4. |
| F5 | MED | Views over out-of-scope/split/invented objects (PlanningBudgets, unsplit Benefits, Timecards, ARReceipts) | **DEFER to catalog-registry refactor (Stage C)** — branches disappear when views select from DMT_CEMLI_CATALOG_TBL. |
| F6 | HIGH | Object registry hardcoded in 6 views, 5 divergent vocabularies | **CORE Stage C work** — this IS the decided catalog-driven refactor (DMT_CEMLI_CATALOG_TBL + registry-backed views). The review turns a planned improvement into a defect inventory; all divergences listed in the full report. |
| F7 | HIGH | ProjectBudgets drill pair byte-broken ('Project Budgets' vs 'Project Budget Lines') | **FIX with catalog refactor** (Stage C) — also flag the spec's internal contradiction (canonical-target table vs appendix C3 fuller names) for user decision. |
| F8 | MED | UNRECONCILED computed 3 ways; Blanket-PO discriminator diverges (phantom-count reintroduced) | **FIX with catalog refactor / derived-classification consolidation** (Stage C; red rule pending). |
| F9 | MED | Decided grains unimplemented: no PARTITION_KEY anywhere; record detail lacks error-table union; missing DMT_PREFIX_HISTORY_V, funnel view | **ALREADY PLANNED** — Stage C (partition/split support) and the funnel work; now cross-referenced. |
| F10 | MED | ESS job detail view: per-file fan-out violates decided grain; TO_CHAR join papers over id-type drift | **DEFER to Stage C ESS wiring** — grain fix with the queue port; id types with the datatype red rule. |
| F11 | MED | Catalog health view doesn't validate ROW_FILTER (the C2/C4 defect class passes health) | **FIX with catalog refactor** (Stage C). |
| F12 | MED | LIKE on controlled codes (1099 filter ×7, OBJECT_TYPE LIKE ×6) | **DEFER to the existing LIKE-audit backlog item** — note: the spec's own section 11 example prescribes the 1099 LIKE filter; spec self-contradiction flagged for user decision. |
| F13 | MED | 82 files on the banned DMT_V_* naming family (79 of them the detail-view family) vs 88 conforming *_V | **DEFER to naming sweep** — rename with their APEX/package references (Stage C/F). |
| F14 | MED | RUN_ID AS SCENARIO_ID in 3 views; detail views name row status RUN_STATUS (altitude conflation) | **DEFER to Stage C sweep** (cross-concept-alias red rule pending). |
| F15 | MED | Status filters drop COMPLETED_ERRORS/NO_ROWS_PROCESSED; invented 'OTC' code; third label vocabulary in scenario summary | **FIX with catalog refactor** (vocabulary red rule pending). |
| F16 | LOW | ROW_FILTER strings feed the page-52 dynamic-SQL violation | **ALREADY PLANNED** — dies with the funnel/catalog redesign. |
| F17 | LOW | All 170 views use CREATE OR REPLACE FORCE | **DEFER — mechanical sweep** (remove FORCE) after red-rule acceptance; must follow the invalid-view cleanups or install breaks (which is the point). |
| F18 | LOW | Mojibake in 5 view files | Covered by mojibake red rule; mechanical sweep. |
| F19 | LOW | Known-gap confirmations (Grants children uncataloged, Workers/WorkRels placement, FBL absent); DMT_RUN_HISTORY_V fully conforming | No action beyond existing backlog. |

# Blind Tranche Review — Tables (2026-07-07)

Reviewer: blind subagent (no build context; read DMT_DESIGN.html in full first).
Scope: all 213 files in `db/tables/` + `db/lookup/schema/` + `db/lookup/coa/` + table lines
of both install scripts.
**Verdict: FAIL.** DMT_OWNER portion alone would be PASS-WITH-FINDINGS (97/97 STG-TFM pairs,
uniform guards, full enrollment, correct FK ordering), but the tranche cannot pass its own
Stage A gate: the DMT_LOOKUP/COA schema is non-idempotent with a permanently-broken statement
masked by blanket error suppression, a committed trigger contains runtime dynamic SQL, one
canonical object's tables are structurally non-conforming, and the seed re-created data-model
defects and retired columns the design explicitly ordered removed.

Proposed rules: 8, added to DMT_DESIGN.html section 7 in RED (pending user review).

## Gate-blocking findings — FIX NOW (self-contained, no package ripple)

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | HIGH | DMT_LOOKUP/COA schema entirely unguarded (bare CREATE/ALTER); install admits "NOT re-runnable" | **FIX NOW** — guard every statement; convert numbered migration scripts to guarded blocks in-place. |
| 2 | HIGH | `DMT_COA_MAP_SRC_N1` duplicates `DMT_COA_MAP_UK` column list (can never succeed); whole COA file wrapped in `whenever sqlerror continue` | **FIX NOW** — delete the dead index statement; remove all `whenever sqlerror continue` from install scripts. |
| 6 | HIGH | Trigger `DMT_LKP_MAPPING_BIU` uses runtime `EXECUTE IMMEDIATE` to read APEX context | **FIX NOW** — replace with static `COALESCE(SYS_CONTEXT('APEX$SESSION','APP_USER'), USER)` (no APEX dependency, no dynamic SQL). |

Gate re-proof required after fixes: `install_dmt_lookup.sql` twice consecutively, zero errors.

## Findings deferred with tracking

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 3 | HIGH | Run table: virtual `SCENARIO_ID AS (RUN_ID+0)` (stored lie); retired INTEGRATION_ID column, CANCELLED status, INCLUDE_UNTAGGED | **DEFER to Stage C engine port (P1)** — dropping columns/constraints must land with the scheduler/queue packages that reference them. Highest-priority deferred item. |
| 4 | HIGH | MiscReceipts family: STG tables missing SCENARIO_ID; lots/serials TFM missing FBDI_CSV_ID | **DEFER to MiscReceipts object port (wired-5, Phase 4)** — schema fix + validator/generator change together. Tracked. |
| 5 | HIGH | FBDI CSV↔ZIP model reproduces the design's "P1 — CSV storage model is wrong (fix ASAP)"; DMT_FBDI_CSV_TBL lacks CEMLI_CODE | **DEFER to Stage B file-generation framework** — the zip/csv relationship inverts when the generator framework ports; fixing the tables first breaks the current generator packages. Tracked P1. |
| 7 | MED | DMT_LOG_TBL missing QUEUE_ID + RUN_ID index; INTEGRATION_ID alias; no sequence default | **DEFER to Stage B (DMT_UTIL_PKG port)** — the log-attribution contract lands with the logging package. Already on the plan (new-DDL list). |
| 8 | MED | WORK_STATUS check allows SPLITTING/GENERATING (unspecified) + VALIDATING (retired); ESS id datatype mismatch vs ESS job table | **DEFER to Stage C queue port** — status vocabulary and datatypes change with the queue packages. |
| 9 | MED | DMT_BIP_REPORT_TBL missing all Contract v1 registry columns; CEMLI_CODE length drift; retired C001 code style in comments | **DEFER to Stage B (RUN_BIP_REPORT consolidation)** — registry columns land with the shared BIP layer. |
| 10 | MED | 20 TFM tables use nullable TFM_STATUS instead of STATUS...'STAGED' NOT NULL | **DEFER to per-object ports** — column rename must land with each object's validator/transformer/reconciler. Covered by proposed infra-dictionary rule. |
| 11 | MED | 67 TFM tables lack FUSION_*_ID; no table has RECON_KEY | **DEFER to per-object ports** — reconciliation columns land with each reconciler (BASE-tier/HDL report work already planned). |
| 12 | MED | Legacy objects installed (PREFIX_MASTER, CONVERSION_MASTER archive, RCV pair, PLAN_BUDGET) | **DEFER, tracked** — same disposition as sequences review findings 3/5/6/7: drop with their callers, one commit per family. |
| 13 | MED | 19 STG tables without scenario FK; ~78 scenario FKs unnamed (SYS_C); no TFM RUN_ID FKs | **DEFER to the FK/constraint sweep** — one dedicated commit naming all constraints per proposed rule; TFM RUN_ID FKs added with engine port. |
| 14 | MED | Tables missing _TBL suffix (SPLIT_CFG, SAVEPOINTS, MIGRATION_LOG, all DMT_LOOKUP/COA tables, APEX_EXPORT_TMP) | **DEFER to naming sweep** — note the spec itself writes "DMT_CEMLI_SPLIT_CFG"; spec inconsistency flagged for user decision when reviewing red rules. |
| 15 | MED | Numbered migration scripts as deploy source; DMT_MIGRATION_LOG institutionalizes it | **PARTIALLY FIXED NOW** (lookup scripts converted to guarded blocks by finding-1 fix); folding into per-table files tracked for the lookup schema restructure. |
| 16 | MED | Plaintext FUSION_PASSWORD column in ERP options table | **DEFER to Stage B credentials work** — the design's credential-override model replaces this column; tracked. |
| 17 | MED | Missing decided control tables: DMT_STG_TFM_ERROR_TBL, DMT_PIPELINE_DEF_TBL, DMT_CEMLI_CATALOG_TBL | **ALREADY PLANNED** — plan section 4, Stage A item A3 lists these as new-build DDL; they are Stage B/C prerequisites, not seed content. |
| 18 | MED | 15 vs 16 GMS_AWD staging tables vs spec | **INVESTIGATE** — same open item as sequences finding 9. |
| 19-24 | LOW | PK mechanism 3 styles; supplier sequence naming; index conventions; unnamed PKs; stale status comments; err$ coverage arbitrary | **DEFER to sweeps** — findings 19/20 now resolved by the ACCEPTED identity-columns rule (2026-07-08): all PKs convert to identity during per-object ports; constraint naming and status vocabulary await their red rules; err$ coverage lands with the set-based DML work in Stage B/D. |
| 25 | LOW | Mojibake in COMMENT literals | **DEFER — mechanical sweep** (one sed pass + pre-commit grep per red rule) after user accepts the rule. |
| 26 | LOW | Audit-column drift (LAST_UPDATE_BY vs LAST_UPDATED_BY; DATE vs TIMESTAMP) | **DEFER to naming sweep**; add to infra-dictionary if accepted. |

## Verified clean
- All 44 file-based canonical objects have STG/TFM pairs present and enrolled; zero orphan
  files; `_foreign_keys.sql` correctly ordered after all tables.
- DMT_OWNER guard style uniform across all 213 files.

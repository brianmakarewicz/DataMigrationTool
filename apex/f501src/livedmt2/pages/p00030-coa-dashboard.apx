page 30 (
    name: COA Dashboard
    alias: COA-DASHBOARD
    title: COA Dashboard
    appearance {
        pageTemplate: @/standard
        templateOptions: #DEFAULT#
    }
    navigation {
        cursorFocus: doNotFocusCursor
    }
    security {
        pageAccessProtection: argumentsMustHaveChecksum
        formAutoComplete: false
    }

    region breadcrumb (
        name: Breadcrumb
        type: breadcrumb
        source {
            breadcrumb: @breadcrumb
        }
        layout {
            sequence: 10
            slot: breadcrumbBar
        }
        appearance {
            template: @/title-bar
            templateOptions: [
                #DEFAULT#
                t-BreadcrumbRegion--useBreadcrumbTitle
            ]
        }
        componentAppearance {
            breadcrumbTemplate: @/breadcrumb
            templateOptions: #DEFAULT#
        }
    )

    region coa-dashboard (
        name: COA Dashboard
        type: interactiveReport
        source {
            location: localDatabase
            type: sqlQuery
            sqlQuery:
                ```sql
                SELECT mc.config_code,
                       mc.description,
                       src.set_code     AS source_coa,
                       src.ledger_name  AS source_ledger,
                       tgt.set_code     AS target_coa,
                       tgt.ledger_name  AS target_ledger,
                       NVL(sc.src_count, 0)  AS source_combos,
                       NVL(mm.map_count, 0)  AS mapped,
                       NVL(sc.src_count, 0) - NVL(mm.map_count, 0) AS unmapped,
                       CASE
                           WHEN NVL(sc.src_count, 0) = 0 THEN 'No Source Data'
                           WHEN NVL(mm.map_count, 0) >= NVL(sc.src_count, 0) THEN 'Complete'
                           WHEN NVL(mm.map_count, 0) > 0 THEN 'In Progress'
                           ELSE 'Not Started'
                       END AS status,
                       ROUND(NVL(mm.map_count, 0) / NULLIF(NVL(sc.src_count, 0), 0) * 100) AS pct_complete,
                       mc.active_flag
                FROM DMT_COA_MAP_CONFIG mc
                JOIN DMT_COA_SET src ON src.coa_set_id = mc.source_coa_set_id
                JOIN DMT_COA_SET tgt ON tgt.coa_set_id = mc.target_coa_set_id
                LEFT JOIN (
                    SELECT coa_set_id, COUNT(*) AS src_count
                    FROM DMT_COA_SOURCE_COMBOS WHERE enabled_flag = 'Y'
                    GROUP BY coa_set_id
                ) sc ON sc.coa_set_id = mc.source_coa_set_id
                LEFT JOIN (
                    SELECT map_config_id, COUNT(*) AS map_count
                    FROM DMT_COA_MAPPING WHERE active_flag = 'Y'
                    GROUP BY map_config_id
                ) mm ON mm.map_config_id = mc.map_config_id
                ORDER BY mc.config_code
                ```
        }
        layout {
            sequence: 10
            slot: body
        }
        appearance {
            template: @/interactive-report
            templateOptions: [
                #DEFAULT#
                t-IRR-region--hideHeader js-addHiddenHeadingRoleDesc
            ]
        }
        pagination {
            type: rowRangesXToY
        }
        messages {
            whenNoDataFound: No data found.
            whenMoreDataFound: The maximum row count for this report is #MAX_ROW_COUNT# rows.  Please apply a filter to reduce the number of records in your query.
        }
        advanced {
            savedReportMappingIdentifier: 50015943079890228
        }

        column ACTIVE_FLAG (
            type: plainText
            heading {
                heading: Active Flag
            }
            layout {
                sequence: 12
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: L
            }
        )

        column CONFIG_CODE (
            type: plainText
            heading {
                heading: Config Code
            }
            layout {
                sequence: 1
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: A
            }
        )

        column DESCRIPTION (
            type: plainText
            heading {
                heading: Description
            }
            layout {
                sequence: 2
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: B
            }
        )

        column MAPPED (
            type: plainText
            heading {
                heading: Mapped
                alignment: end
            }
            layout {
                sequence: 8
                columnAlignment: end
            }
            source {
                dataType: NUMBER
            }
            advanced {
                endUserAlias: H
            }
        )

        column PCT_COMPLETE (
            type: plainText
            heading {
                heading: Pct Complete
                alignment: end
            }
            layout {
                sequence: 11
                columnAlignment: end
            }
            source {
                dataType: NUMBER
            }
            advanced {
                endUserAlias: K
            }
        )

        column SOURCE_COA (
            type: plainText
            heading {
                heading: Source Coa
            }
            layout {
                sequence: 3
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: C
            }
        )

        column SOURCE_COMBOS (
            type: plainText
            heading {
                heading: Source Combos
                alignment: end
            }
            layout {
                sequence: 7
                columnAlignment: end
            }
            source {
                dataType: NUMBER
            }
            advanced {
                endUserAlias: G
            }
        )

        column SOURCE_LEDGER (
            type: plainText
            heading {
                heading: Source Ledger
            }
            layout {
                sequence: 4
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: D
            }
        )

        column STATUS (
            type: plainText
            heading {
                heading: Status
            }
            layout {
                sequence: 10
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: J
            }
        )

        column TARGET_COA (
            type: plainText
            heading {
                heading: Target Coa
            }
            layout {
                sequence: 5
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: E
            }
        )

        column TARGET_LEDGER (
            type: plainText
            heading {
                heading: Target Ledger
            }
            layout {
                sequence: 6
            }
            source {
                dataType: STRING
            }
            advanced {
                endUserAlias: F
            }
        )

        column UNMAPPED (
            type: plainText
            heading {
                heading: Unmapped
                alignment: end
            }
            layout {
                sequence: 9
                columnAlignment: end
            }
            source {
                dataType: NUMBER
            }
            advanced {
                endUserAlias: I
            }
        )

        page {
            borderWidth: 0.5
            borderColor: #666666
        }
        pageHeader {
            pageHeaderText: COA Dashboard
        }

        savedReport 500938 (
            visibility: primaryDefault
            view {
                rowsPerPage: 50
            }

            displayColumn ACTIVE_FLAG (
                sequence: 12
            )

            displayColumn CONFIG_CODE (
                sequence: 1
            )

            displayColumn DESCRIPTION (
                sequence: 2
            )

            displayColumn MAPPED (
                sequence: 8
            )

            displayColumn PCT_COMPLETE (
                sequence: 11
            )

            displayColumn SOURCE_COA (
                sequence: 3
            )

            displayColumn SOURCE_COMBOS (
                sequence: 7
            )

            displayColumn SOURCE_LEDGER (
                sequence: 4
            )

            displayColumn STATUS (
                sequence: 10
            )

            displayColumn TARGET_COA (
                sequence: 5
            )

            displayColumn TARGET_LEDGER (
                sequence: 6
            )

            displayColumn UNMAPPED (
                sequence: 9
            )

        )

    )

    region summary-cards (
        name: Summary Cards
        type: plSqlDynamicContent
        source {
            plsqlCode:
                ```plsql
                DECLARE
                  v_total NUMBER;
                  v_mapped NUMBER;
                  v_unmapped NUMBER;
                  v_configs NUMBER;
                BEGIN
                  SELECT COUNT(*) INTO v_total FROM DMT_LOOKUP.DMT_COA_SOURCE_COMBOS WHERE enabled_flag = 'Y';
                  SELECT COUNT(*) INTO v_mapped FROM DMT_LOOKUP.DMT_COA_MAPPING WHERE active_flag = 'Y';
                  v_unmapped := v_total - v_mapped;
                  SELECT COUNT(*) INTO v_configs FROM DMT_LOOKUP.DMT_COA_MAP_CONFIG WHERE active_flag = 'Y';
                  HTP.P('<div style="display:flex;gap:12px;margin:12px 0;flex-wrap:wrap;">');
                  HTP.P('<div style="flex:1;min-width:140px;padding:16px;background:#f0f4ff;border-radius:8px;border:1px solid #c0d2ff;"><div style="color:#5a6f8c;font-size:12px;">SOURCE COMBOS</div><div style="font-size:28px;font-weight:600;color:#1a3661;">'||v_total||'</div></div>');
                  HTP.P('<div style="flex:1;min-width:140px;padding:16px;background:#e8f6e9;border-radius:8px;border:1px solid #bfe2c3;"><div style="color:#3a6b40;font-size:12px;">MAPPED</div><div style="font-size:28px;font-weight:600;color:#1f5226;">'||v_mapped||'</div></div>');
                  HTP.P('<div style="flex:1;min-width:140px;padding:16px;background:#fff4e6;border-radius:8px;border:1px solid #ffd9a8;"><div style="color:#8c5a1a;font-size:12px;">UNMAPPED</div><div style="font-size:28px;font-weight:600;color:#6b4111;">'||v_unmapped||'</div></div>');
                  HTP.P('<div style="flex:1;min-width:140px;padding:16px;background:#e6f5fb;border-radius:8px;border:1px solid #b0d9eb;"><div style="color:#1f6082;font-size:12px;">ACTIVE CONFIGS</div><div style="font-size:28px;font-weight:600;color:#0e3a52;">'||v_configs||'</div></div>');
                  HTP.P('</div>');
                END;
                ```
        }
        layout {
            sequence: 5
            parentRegion: @coa-dashboard
            slot: regionBody
        }
        appearance {
            template: @/standard
            templateOptions: [
                #DEFAULT#
                t-Region--scrollBody
            ]
        }
    )

    button generate-all (
        buttonName: GENERATE_ALL
        label: Generate All Mappings
        layout {
            sequence: 20
            region: @coa-dashboard
            slot: regionBody
            startNewRow: false
            newColumn: false
        }
        appearance {
            buttonTemplate: @/text
            templateOptions: #DEFAULT#
        }
        behavior {
            warnOnUnsavedChanges: doNotCheck
        }
    )

    button validate-all (
        buttonName: VALIDATE_ALL
        label: Validate All
        layout {
            sequence: 20
            region: @coa-dashboard
            slot: regionBody
            startNewRow: false
            newColumn: false
        }
        appearance {
            buttonTemplate: @/text
            templateOptions: #DEFAULT#
        }
        behavior {
            warnOnUnsavedChanges: doNotCheck
        }
    )

    process generate-all-mappings (
        name: Generate All Mappings
        type: executeCode
        source {
            plsqlCode:
                ```plsql
                DECLARE
                    v_total NUMBER := 0;
                    v_cnt NUMBER;
                BEGIN
                    FOR rec IN (SELECT config_code FROM DMT_COA_MAP_CONFIG WHERE active_flag = 'Y' ORDER BY config_code) LOOP
                        v_cnt := DMT_COA_MAP_PKG.GENERATE_MAPPINGS(rec.config_code);
                        v_total := v_total + v_cnt;
                    END LOOP;
                    APEX_APPLICATION.G_PRINT_SUCCESS_MESSAGE := v_total || ' total mappings generated across all active configs.';
                END;
                ```
        }
        execution {
            sequence: 10
        }
        advanced {
            executionMappingIdentifier: 50402370563094401
        }
        serverSideCondition {
            whenButtonPressed: @generate-all
        }
    )

    process validate-all-mappings (
        name: Validate All Mappings
        type: executeCode
        source {
            plsqlCode:
                ```plsql
                DECLARE
                    v_total NUMBER := 0;
                    v_cnt NUMBER;
                BEGIN
                    FOR rec IN (SELECT config_code FROM DMT_COA_MAP_CONFIG WHERE active_flag = 'Y' ORDER BY config_code) LOOP
                        v_cnt := DMT_COA_MAP_PKG.VALIDATE_MAPPINGS(rec.config_code);
                        v_total := v_total + v_cnt;
                    END LOOP;
                    APEX_APPLICATION.G_PRINT_SUCCESS_MESSAGE := v_total || ' total invalid mappings flagged across all active configs.';
                END;
                ```
        }
        execution {
            sequence: 20
        }
        advanced {
            executionMappingIdentifier: 50402413490094402
        }
        serverSideCondition {
            whenButtonPressed: @validate-all
        }
    )

)


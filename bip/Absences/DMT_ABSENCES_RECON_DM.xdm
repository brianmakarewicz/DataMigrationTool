<?xml version="1.0" encoding="utf-8"?>
<dataModel xmlns="http://xmlns.oracle.com/oxp/xmlp" version="2.1"
           defaultDataSourceRef="ApplicationDB_FSCM">
  <!--
    DMT_ABSENCES_RECON_DM — Contract v1 reconciliation data model for Absences
    (design section 5, "BIP reconciliation report contract - v1"). Copied EXACTLY
    from the merged Workers template DMT_WORKERS_RECON_DM.xdm (PR #248).

    Returns the BASE tier for the Absences HDL load: one row per migrated
    absence entry that is positively confirmed in the Fusion HCM base table
    ANC_PER_ABS_ENTRIES, with its real Fusion PER_ABSENCE_ENTRY_ID as FUSION_ID.
    HDL failures arrive per-record in the HDL response (tagged [FUSION_ERROR] by
    RECONCILE_HDL before this report runs), so this report's ERROR rows are
    optional and it returns the BASE tier only — the Absences reconciler (which
    fetches these rows via DMT_RECON_CONTRACT_PKG.FETCH_ROWS and applies them
    statically) promotes an Absence TFM row to LOADED only from a
    BASE / SUCCESS / FUSION_ID-not-null row here.

    RECORD_KEY = the absence entry's business key = the value written into
    PersonAbsenceEntry.dat as SourceSystemId, which is the prefixed PERSON_NUMBER
    with the '_ABS' record suffix (DMT_ABSENCE_HDL_GEN_PKG writes
    PERSON_NUMBER || '_ABS'), stamped on the Absence TFM row as RECON_KEY (one
    key definition per object). Unlike Workers (which have a business column,
    PERSON_NUMBER, on the base table), an absence entry has NO business column on
    ANC_PER_ABS_ENTRIES, so the tie from our written SourceSystemId to the base
    row is the HCM integration key map HRC_INTEGRATION_KEY_MAP:
      HRC_INTEGRATION_KEY_MAP.SOURCE_SYSTEM_ID = the SourceSystemId we wrote
      HRC_INTEGRATION_KEY_MAP.SURROGATE_ID     = ANC_PER_ABS_ENTRIES.PER_ABSENCE_ENTRY_ID
    for OBJECT_NAME = 'PersonAbsenceEntry'.

    Verified live 2026-09-16 (fin_impl):
      * HRC_INTEGRATION_KEY_MAP has 8701 rows for OBJECT_NAME='PersonAbsenceEntry',
        exactly matching COUNT(*) of ANC_PER_ABS_ENTRIES (8701), and every one
        joins SURROGATE_ID = PER_ABSENCE_ENTRY_ID.
      * SOURCE_SYSTEM_ID LIKE <prefix>||'%' filters correctly and the base join
        holds.

    Six Contract v1 parameters; keyset pagination by RECORD_KEY (P_AFTER_KEY).
    P_LOAD_REQUEST_ID / P_IMPORT_ESS_ID do not survive into the base table for an
    HDL load, so BASE-tier matching is by the run prefix (P_PREFIX) on the map's
    SOURCE_SYSTEM_ID. LOAD_REQUEST_ID is echoed back unchanged for audit.
  -->
  <dataProperties>
    <property name="include_parameters" value="true"/>
    <property name="include_null_Element" value="true"/>
    <property name="include_rowsettag" value="false"/>
    <property name="xml_tag_case" value="upper"/>
  </dataProperties>
  <dataSets>
    <dataSet name="absences_recon" type="complex">
      <sql dataSourceRef="ApplicationDB_FSCM"><![CDATA[
SELECT object_type, record_key, source_type, fusion_status,
       fusion_id, error_message, load_request_id
FROM (
    -- BASE tier: positive proof in the HCM absence-entry base table, tied to our
    -- written SourceSystemId through the HCM integration key map. One row per
    -- migrated absence entry.
    SELECT 'Absences'                      AS object_type,
           m.source_system_id              AS record_key,
           'BASE'                          AS source_type,
           'SUCCESS'                       AS fusion_status,
           MAX(e.per_absence_entry_id)     AS fusion_id,
           CAST(NULL AS VARCHAR2(4000))    AS error_message,
           :P_LOAD_REQUEST_ID              AS load_request_id
    FROM   hrc_integration_key_map m
    JOIN   anc_per_abs_entries e
           ON e.per_absence_entry_id = m.surrogate_id
    WHERE  m.object_name = 'PersonAbsenceEntry'
    AND    m.source_system_id LIKE :P_PREFIX || '%'
    AND    (:P_AFTER_KEY IS NULL OR m.source_system_id > :P_AFTER_KEY)
    GROUP BY m.source_system_id
    ORDER BY m.source_system_id
)
WHERE ROWNUM <= :P_CHUNK_SIZE
      ]]></sql>
    </dataSet>
  </dataSets>
  <output rootName="DATA_DS" uniqueRowName="false">
    <nodeList name="data-structure">
      <dataStructure tagName="DATA_DS">
        <group name="G_1" label="G_1" source="absences_recon">
          <element name="OBJECT_TYPE"     value="OBJECT_TYPE"     dataType="xsd:string"  tagName="OBJECT_TYPE"/>
          <element name="RECORD_KEY"      value="RECORD_KEY"      dataType="xsd:string"  tagName="RECORD_KEY"/>
          <element name="SOURCE_TYPE"     value="SOURCE_TYPE"     dataType="xsd:string"  tagName="SOURCE_TYPE"/>
          <element name="FUSION_STATUS"   value="FUSION_STATUS"   dataType="xsd:string"  tagName="FUSION_STATUS"/>
          <element name="FUSION_ID"       value="FUSION_ID"       dataType="xsd:integer" tagName="FUSION_ID"/>
          <element name="ERROR_MESSAGE"   value="ERROR_MESSAGE"   dataType="xsd:string"  tagName="ERROR_MESSAGE"/>
          <element name="LOAD_REQUEST_ID" value="LOAD_REQUEST_ID" dataType="xsd:string"  tagName="LOAD_REQUEST_ID"/>
        </group>
      </dataStructure>
    </nodeList>
  </output>
  <eventTriggers/>
  <lexicals/>
  <parameters>
    <parameter name="P_RUN_ID" dataType="character" rowPlacement="1"
               labelID="P_RUN_ID" defaultValue="" nvp="false"
               refreshParamOnChange="false" useNullForAll="false"/>
    <parameter name="P_LOAD_REQUEST_ID" dataType="character" rowPlacement="2"
               labelID="P_LOAD_REQUEST_ID" defaultValue="" nvp="false"
               refreshParamOnChange="false" useNullForAll="false"/>
    <parameter name="P_IMPORT_ESS_ID" dataType="character" rowPlacement="3"
               labelID="P_IMPORT_ESS_ID" defaultValue="" nvp="false"
               refreshParamOnChange="false" useNullForAll="false"/>
    <parameter name="P_PREFIX" dataType="character" rowPlacement="4"
               labelID="P_PREFIX" defaultValue="" nvp="false"
               refreshParamOnChange="false" useNullForAll="false"/>
    <parameter name="P_CHUNK_SIZE" dataType="integer" rowPlacement="5"
               labelID="P_CHUNK_SIZE" defaultValue="5000" nvp="false"
               refreshParamOnChange="false" useNullForAll="false"/>
    <parameter name="P_AFTER_KEY" dataType="character" rowPlacement="6"
               labelID="P_AFTER_KEY" defaultValue="" nvp="false"
               refreshParamOnChange="false" useNullForAll="false"/>
  </parameters>
  <valueSets/>
  <bursting/>
</dataModel>
